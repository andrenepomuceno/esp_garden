.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
data/config.json
include/config.backup.h
backups/

# dev_server.py imports its sim_* siblings now, so Python caches them here.
__pycache__/

# deploy/.env holds the real VPS credentials; deploy/.env.example is the tracked template.
deploy/.env

# ThingsBoard read credential, kept out of the tree on purpose. Untracked is
# not enough: a stray `git add` would publish it to a remote.
.chave_tb_*
