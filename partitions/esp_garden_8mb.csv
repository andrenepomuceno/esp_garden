# Partition table for the esp-garden-hardware carrier (ESP32-S3, 8 MB flash).
#
# The ESP32-S3-DevKitC-1-N8R8 this board sockets has 8 MB of flash. Running it
# on partitions/esp_garden_4mb.csv works and strands half the part, which would
# be a cheap mistake to make and an expensive one to correct: a partition table
# cannot be delivered over OTA — the running image would be rewriting the map
# underneath itself — so the layout a board is first flashed with is the layout
# it keeps until somebody attaches USB to it again. That is the whole reason to
# get it right on a board that has not been built yet.
#
#   app0/app1  0x2C0000 = 2.75 MB each
#   spiffs     0x260000 = 2.375 MB   (LittleFS; the subtype name is historical
#                                     and is what LittleFS.begin() looks for)
#   coredump   0x10000  = 64 KB      (esptool read_flash 0x7F0000 0x10000)
#
# The app slots are sized from need, not from what is left over. An S3 image of
# this firmware is larger than the ESP32 one, and the 4 MB table's 1.69 MB slot
# would still have fitted it — 2.75 MB is headroom bought with flash that has
# no other claimant, not a measurement of anything.
#
# The filesystem is the same argument, and the numbers are worth stating in
# whole 4 KB LittleFS blocks rather than in bytes of data — an earlier version
# of this paragraph did the latter and understated the history by more than
# half. Measured, not estimated: the staged web assets are 28 files, 148 KB of
# data and 212 KB once each is rounded up to a block; history.records is capped
# at 5000 by a compiled constant this file does not change, and 5000 records is
# 256 KB (8 * ceil((12 + 625 * 48) / 4096) * 4096), not the 120 KB this said
# while quoting a 2500 that had already been raised.
#
# The reserve ioHistoryFitCapacity() holds back is max(64 KB, partition / 8),
# and THIS is the first partition on which the proportional term wins: 304 KB
# here against 64 KB on every 4 MB board, where both terms are 64 KB and the
# "larger of" has never had to choose.
#
# So the worst case this layout has to hold is 212 + 256 + 304 = 772 KB of
# 2432 KB. The sizing argument survives the correction with room to spare — the
# filesystem is about three times the largest thing that can be asked of it —
# and it is still headroom rather than a measured need: the point is that the
# record cap can be raised later WITHOUT a serial reflash, which is the one
# thing the 4 MB boards cannot do.
#
# TWO OTA SLOTS ARE DELIBERATE, for the reason esp_garden_4mb.csv gives: a
# single app partition would buy ~5.5 MB and make the device un-updatable over
# the network.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x2C0000,
app1,       app,  ota_1,    0x2D0000, 0x2C0000,
spiffs,     data, spiffs,   0x590000, 0x260000,
coredump,   data, coredump, 0x7F0000, 0x10000,
